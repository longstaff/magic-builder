import React from 'react';
import { ModalBox, ModalOverlay, SectionTitle } from '../Css';


const LoadingModal = () => <ModalOverlay>
		<ModalBox>
			<SectionTitle>Loading</SectionTitle>
		</ModalBox>
	</ModalOverlay>

export default LoadingModal